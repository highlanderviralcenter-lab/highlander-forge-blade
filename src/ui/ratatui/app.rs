//! RatatuiApp — implementacao do trait Ui para ratatui

use crate::app::messages::AppMsg;
use crate::app::state::AppState;

pub struct RatatuiApp;

impl RatatuiApp {
    pub fn new() -> Self {
        Self
    }
}
